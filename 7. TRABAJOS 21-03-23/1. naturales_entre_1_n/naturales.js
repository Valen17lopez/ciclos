/*
Mostrar los naturales 1 y n
*/
let n= parseInt(prompt("Ingrese un numero"));
let contador = 1;
//se verifica la condicion, si contador es menor o igual que n
while (contador <= n) {
    console.log(contador)
    contador++
    // contador++ es lo mismo que contador = contador+1
    
}